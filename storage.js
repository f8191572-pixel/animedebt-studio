const KEY="animedebt_projects";
export function isLocalStorageAvailable(){try{localStorage.setItem("__ad_test__","1");localStorage.removeItem("__ad_test__");return true}catch{return false}}
export function getProjects(){if(!isLocalStorageAvailable())return[];try{const p=JSON.parse(localStorage.getItem(KEY))||[];return Array.isArray(p)?p.sort((a,b)=>new Date(b.updatedAt||0)-new Date(a.updatedAt||0)):[]}catch{return[]}}
export function saveProject(project){if(!isLocalStorageAvailable())throw new Error("localStorage is not available in this browser.");const projects=getProjects();const now=new Date().toISOString();const item={...project,id:project.id||crypto.randomUUID(),createdAt:project.createdAt||now,updatedAt:now};const i=projects.findIndex(p=>p.id===item.id);if(i>=0)projects[i]=item;else projects.unshift(item);localStorage.setItem(KEY,JSON.stringify(projects));return item}
export function deleteProject(id){if(!isLocalStorageAvailable())throw new Error("localStorage is not available in this browser.");const projects=getProjects().filter(p=>p.id!==id);localStorage.setItem(KEY,JSON.stringify(projects));return projects}
export function exportProject(project){return JSON.stringify({exportedAt:new Date().toISOString(),app:"AnimeDebt Studio",version:"phase-7",project},null,2)}
export function normalizeImportedProject(payload){const raw=payload?.project||payload;if(!raw||typeof raw!=="object")throw new Error("Invalid project JSON.");if(!raw.storyIdea&&!raw.title)throw new Error("This JSON does not look like an AnimeDebt project.");return{...raw,id:crypto.randomUUID(),importedAt:new Date().toISOString()}}